# Hawk Eye pipeline configuration.

Passes:
  -
    type: rasterized
    post:
      #- blur
    shaders:
        vertex: ../../src/shaders/test.vert.glsl
        fragment: ../../src/shaders/test.frag.glsl
    targets:
      - color
      #- depth
      #- sample
    samples: 1
    vertex attributes:
        # position
      - vec3
        # normal
      #- vec3
        # color
      #- vec3
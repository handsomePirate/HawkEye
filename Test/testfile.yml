# Hawk Eye pipeline configuration.

Layers:
  -
    type: rasterized
    post:
      #- blur
    shaders:
        vertex: ../../src/shaders/test.vert.glsl
        fragment: ../../src/shaders/test.frag.glsl
    targets:
      - color
      #- depth
      #- sample
    samples: 1
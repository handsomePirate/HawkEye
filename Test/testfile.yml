# Hawk Eye pipeline configuration.

Layers:
  -
    type: computed
    post:
      #- blur
    shaders:
        compute: simple.comp.glsl
    targets:
      - color
      #- depth
      #- sample
    samples: 1
# Hawk Eye pipeline configuration.

Passes:
  -
    type: rasterized
    post:
      #- blur
    shaders:
        vertex: ../../src/shaders/test.vert.glsl
        fragment: ../../src/shaders/test.frag.glsl
    targets:
      - color
      #- depth
      #- sample
    samples: 1
    # TODO: Change to descriptors.
    uniforms:
        # in bytes
      - 
        name: test
        size: 4
        type: uniform
      -
        name: complex
        size: 8
        type: uniform
      -
        name: texture
        type: texture
    vertex attributes:
        # position
      - vec3
        # normal
      #- vec3
        # color
      - vec3
        # uv
      - vec2